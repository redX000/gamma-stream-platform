# Gamma Control Center — Admin credentials
# Copy to .env and set your own values. NEVER commit .env.

# Admin login credentials (both required)
VITE_ADMIN_USER=lisa
VITE_ADMIN_PASS=gamma_admin_2026

# Optional: API base URL when wiring to real backend
# VITE_API_BASE_URL=https://api.gammacash.online
